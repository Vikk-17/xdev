const express = require("express");
const app = express();
const port = 3000;

// get the timestamp of every request
let numberOfRequest = {};

const timePerReq = setInterval(() => {
  
}, 1000);







app.listen(3000, () => {
  console.log("Server is running on port ${port}");
});
